spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    health:
      show-details: always

info:
  app:
    version: 1.0

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
